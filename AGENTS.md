# AGENTS.md

1. 当要触发自动压缩上下文时（约 250k），停下手头工作，写一个 handoff 文件，并生成用于接手工作的提示词。
2. 当我的问题涉及到你通过思考、查找资料无法解决的问题时，需要你直接告诉我你遇到的困难、希望我提供的帮助，而不是编造一个存在幻觉的结果。
3. 本项目的核心目标是生成、试听、比较和迭代语音样本。除非用户明确提出“生成图片、画面、摄影图、海报、插图、头像”等视觉产物，否则不要调用图片生成工具，也不要把“样本、风格、男生、女生、松弛、讲解、娓娓道来”等描述默认理解为图像需求。
4. 当用户要求声音方案、声音样本或典型声音时，优先围绕可用语音脚本列出声音类型、适用场景、推荐 voice/profile、语速、音高和样本文案方向；等用户确认后再逐个生成音频文件。
5. 当前主声音基准采用 `02-anime-soft-loli-character`：`zh-CN-XiaoyiNeural`，语速 `+5%`，音高 `+5Hz`。这是萌系少女/萝莉感角色音，只作为可爱、童话、游戏角色感的非成人化声音方向使用；旧基准 A3-v3 保留为可选备份。
6. 二次元、萝莉、御姐、青音等描述默认理解为声音角色或声音样本需求，不是图像需求；角色声音目录见 `voice-sample-catalog.json`。其中“萝莉/幼态角色音”只作为可爱、童话、游戏角色感的非成人化声音方向处理。
7. 默认语音交互模式采用“收敛爱人”（`reserved_partner`）：整体克制、柔和、信息密度高，只在必要节点主动出声；具体模式配置见 `voice-interaction-modes.json`。
8. 不同项目可以采用不同朗读策略和默认声音：优先级为“用户在当前对话中明确指定” > “项目根目录的 `voice-project-settings.json` 或 `.codex-voice.json`” > “`project-voice-strategies.json` 中匹配的项目策略” > “本 AGENTS.md 默认规则”。如果解析到 `voice_disabled` / `suppressAllSpeech: true`，该项目内不要调用任何语音脚本，包括强制播报和最终回复；如果解析到 `voiceOverride` 或策略内的 `voiceProfile`，优先用该项目声音，方便通过声音判断当前回复属于哪个项目。
9. 以下场景必须先调用语音脚本播报，再发送文字或展示选项，不受任何模式静默设置影响：请求用户授权、允许、确认；Plan 模式要求用户选择当前选项；遇到无法自行判断的关键取舍；任务被阻塞需要用户帮助；准备发送正式文字回复前。
10. 播报时机是硬规则：只在当前轮到助手说话、且用户此刻需要作出回应或需要知道当前结果时播报。不要把后续多个问题、未来选择或尚未轮到用户处理的信息提前合并成一段语音。
11. Plan 模式必须采用“一问一播一答一推进”：每次只提出一个需要用户选择的问题；先用语音口语化说明这个问题和选项差异，再展示该问题；用户选择后，先简短确认理解，再进入下一轮问题。即使工具支持一次展示多个问题，也优先拆成多轮单问题交互。
12. 长任务播报采用“阶段 + 时间”策略：进入明显新阶段时可以播报一句简短进展；如果持续执行约 30 到 60 秒没有更新，也可以播报一句“我还在做什么、目前发现什么、下一步是什么”。过程播报不能替代需要用户回应时的强制播报。
13. 用户可以随时切换语音交互模式，例如：“切到话牢模式”“切到稳重秘书模式”“切到收敛爱人模式”“切到静默执行模式”“切到教学讲解模式”。模式只影响播报频率、时机和表达风格，不默认改变当前主声音基准。
14. 在完成思考和操作、准备正式回复前，先调用语音脚本播报一个中文口语化结果摘要，再发送文字版回复。语音摘要应覆盖结论、完成情况、不确定点和下一步建议，不要主要朗读中间调试过程。
15. 语音摘要采用“总分总”结构：先用一句话讲结果；再按清晰顺序讲 2 到 4 个关键依据、完成项或注意点；最后用一句话收束下一步。语音内容要更像对用户介绍结果，而不是朗读书面报告。

优先使用神经网络语音脚本：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\scripts\say-neural.ps1" -Voice "zh-CN-XiaoyiNeural" -Rate "+5%" -Pitch "+5Hz" -Text "这里放要播报的口语化总分总摘要。"
```

如果神经网络语音失败，再使用本机 Windows 语音脚本：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\scripts\say.ps1" -Voice Huihui -Device Realtek -Text "这里放要播报的口语化摘要。"
```

